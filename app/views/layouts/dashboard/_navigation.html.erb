<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'MDwriter', root_path, class: 'navbar-brand' %>
    </div>
    <div class="collapse navbar-collapse">
      <% if @current_blog %>
      <p class="navbar-text"> &gt; </p>
      <p class="navbar-text"><%= link_to @current_blog.title, dashboard_path(@current_blog), :class => 'navbar-link' %></p>
      <% end %>
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.name %><span class="caret"></span></a>
        <ul class="dropdown-menu" role="menu">
          <% if current_user.admin? %>
            <li><%= link_to 'Users', users_path %></li>
          <% end %>
          <li><%= link_to 'Edit account', edit_user_registration_path %></li>
          <li class="divider"></li>
          <li><%= link_to 'Sign out', destroy_user_session_path, :method=>'delete' %></li>
        </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>
