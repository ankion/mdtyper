  <aside>
    <div id="sidebar-wrapper">
      <ul class="sidebar-nav">
        <li class="sidebar-brand">Blog Categories</li>
        <% @categories.each do |category| %>
          <li><a href="#"><%= category.name %> (<%= category.post_categoryships_count %>)</a></li>
        <% end %>
      </ul>
    </div>
    <!-- /#sidebar-wrapper -->
  </aside>
